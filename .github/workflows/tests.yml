name: Tests

on:
  push:
    branches: [main]
  pull_request:
  workflow_call:

permissions:
  contents: read

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: '8.5'
          extensions: mysqli, mbstring, sodium
          coverage: none
          tools: none

      - name: PHP syntax
        run: find . -name '*.php' -not -path './vendor/*' -print0 | xargs -0 -n1 php -l > /dev/null

      - name: JavaScript syntax
        run: node --check public/assets/app.js

      - name: Validate composer.json
        run: composer validate --strict --no-check-publish

      - name: Release archive builds
        run: |
          build/release.sh 0.0.0-ci /tmp/dist
          unzip -l /tmp/dist/db-simply-0.0.0-ci.zip

  test:
    name: PHP ${{ matrix.php }} · ${{ matrix.server }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ['8.3', '8.4', '8.5']
        server: ['mariadb:11.4']
        include:
          - php: '8.5'
            server: 'mariadb:10.6'
          - php: '8.5'
            server: 'mariadb:10.11'
          - php: '8.5'
            server: 'mysql:8.0'
          - php: '8.5'
            server: 'mysql:8.4'

    services:
      database:
        image: ${{ matrix.server }}
        ports:
          - 3306:3306
        # Both images read these. The test user gets all privileges on the
        # test database only, as an account's user does on a server.
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: dbsimply_test
          MYSQL_USER: tester
          MYSQL_PASSWORD: secret

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: ${{ matrix.php }}
          extensions: mysqli, mbstring, sodium
          coverage: none
          tools: none

      - name: Unit tests
        run: php tests/run.php

      - name: Wait for the database
        run: |
          for i in $(seq 1 60); do
            php -r 'mysqli_report(MYSQLI_REPORT_OFF); exit(@mysqli_connect("127.0.0.1", "tester", "secret", "dbsimply_test", 3306) ? 0 : 1);' && exit 0
            sleep 2
          done
          echo "The database did not come up." >&2
          exit 1

      # MySQL with binary logging refuses to create triggers for a user
      # without SUPER unless told to trust them, as a hosting server would be.
      - name: Allow triggers without SUPER
        if: startsWith(matrix.server, 'mysql')
        run: php -r '$db = new mysqli("127.0.0.1", "root", "root", "", 3306); $db->query("SET GLOBAL log_bin_trust_function_creators = 1");'

      - name: Integration tests
        run: php tests/run.php
        env:
          DB_SIMPLY_TEST_HOST: 127.0.0.1
          DB_SIMPLY_TEST_PORT: 3306
          DB_SIMPLY_TEST_USER: tester
          DB_SIMPLY_TEST_PASSWORD: secret
          DB_SIMPLY_TEST_DATABASE: dbsimply_test
